#include <bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);

    int t_c;
    cin >> t_c;
    for(int tc=0; tc<t_c; tc++){
        int n,m;
        cin >> n >> m;
        for(int i=0; i<m; i++){
            int a,b;
            cin >> a >> b;
        }
        cout << n-1 << "\n";
    }
}